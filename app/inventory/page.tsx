import { Metadata } from "next";
import InventoryCanvas from "./inventory-canvas";

export const metadata: Metadata = {
  title: "Inventory | Rohan's Stack",
  description: "Things I use, love, and can't live without",
};

export default function InventoryPage() {
  return <>In progress</>;
}
